# MongoDB Configuration - uses environment variable in production
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/job_portal}
spring.data.mongodb.auto-index-creation=true

# Server Configuration - uses PORT environment variable in production
server.port=${PORT:8080}

# Logging Configuration
logging.level.org.springframework.data.mongodb=DEBUG

# Swagger Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

# JWT Configuration
jobportal.app.jwtSecret=${JWT_SECRET:MWJhOTI5NmVjNDRlODEzNTUyOTY2M2MwZDY4NjE4N2UxMjg4MDllNjM4MTU1ZTJhZGY5ZDE1YWI0MzY2YTc5Mg==}
jobportal.app.jwtExpirationMs=86400000

# CORS Configuration (for frontend if needed)
spring.mvc.cors.allowed-origins=*
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*